import profile from '../assets/images/profile.png';

const Header = () => {
    return (
        <header>
            <img src={profile} alt="profile" />
        </header>
    );
};

export default Header;